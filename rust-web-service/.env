# SQLite database file (relative path; created automatically on first run)
DATABASE_URL=sqlite:tasks.db

# Server bind address
HOST=127.0.0.1
PORT=9090

# Number of actix-web worker threads (defaults to 4)
WORKERS=4

# Log level: error | warn | info | debug | trace
RUST_LOG=info
